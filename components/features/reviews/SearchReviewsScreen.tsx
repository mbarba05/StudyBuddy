import React from "react";
import { Text, View } from "react-native";

const SearchReviewsScreen = () => {
    return (
        <View className="flex-1 bg-colors-background">
            <Text>SearchReviewsScreen</Text>
        </View>
    );
};

export default SearchReviewsScreen;
